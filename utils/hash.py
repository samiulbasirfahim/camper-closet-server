# Gotta implement hashing algorithms from previous backend

def hash_password(password: str) -> str:
    return password


def compare_password(password: str, hash: str) -> bool:
    return password == hash
